# Mock mode is the default when MOCK_MODE is unset.
MOCK_MODE=true

# Live mode:
# MOCK_MODE=false
# LLM_HARNESS=claude
# LLM_MODEL=sonnet

# Alternative live harness:
# LLM_HARNESS=codex
# LLM_MODEL=gpt-5.4
# LLM_REASONING_EFFORT=low

# The selected CLI must be installed and authenticated.
